{
  "name": "oakflowers",
  "license": "ISC",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-fe": "cd frontend && yarn",
    "install-be": "cd backend && yarn",
    "build-fe": "cd frontend && yarn build",
    "fe": "cd frontend && yarn start",
    "be": "cd backend && yarn start",
    "dev": "concurrently --kill-others-on-fail \"yarn be\" \"yarn fe\"",
    "prod": "yarn build-fe && cross-env NODE_ENV=test yarn be",
    "deploy": "yarn install-fe && yarn build-fe && rm /var/www/build && mv ./frontend/build /var/www"
  },
  "author": "Philip Ekblom",
  "devDependencies": {
    "concurrently": "^4.1.0"
  },
  "dependencies": {
    "cross-env": "^5.2.0"
  }
}
